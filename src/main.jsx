import React from 'react';
import ReactDOM from 'react-dom/client';
import { LineProject } from './components/LineProject';

ReactDOM.createRoot(document.getElementById('root')).render(
	<React.StrictMode>
		<LineProject />
	</React.StrictMode>
);
